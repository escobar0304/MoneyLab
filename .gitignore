node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo

# Playwright output. Traces and screenshots are per-run debris; the specs
# themselves live in e2e/ and are committed.
playwright-report/
test-results/
blob-report/
/playwright/.cache/

# A local Playwright config, for running the e2e suite against a browser that
# is already on the machine instead of one `playwright install` fetched. It
# pins an absolute executablePath, so it is true for exactly one machine and
# would break `npm run test:e2e` for everyone else if it were committed.
playwright.local.config.ts

# Claude Code project config (local paths/permissions, not meant to be shared)


# Secrets / environment
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*credentials*
*secret*
